version: '3.9'

services:
  next-app-layout:
    tty: true
    stdin_open: true
    volumes:
      - .:/app
    command: >
      sh -c "yarn install && yarn dev"
    build:
      context: .
      dockerfile: next.Dockerfile
    ports:
      - "4000:4000"